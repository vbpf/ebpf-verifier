#include "config.hpp"

global_options_t global_options {
    .simplify = false,
    .stats = false,
    .check_semantic_reachability = false,
    .print_invariants = true,
    .print_failures = true,
    .liveness = true
};
